name: flutter_expandable_column
description: "An expandable Flutter column widget that supports scrolling while allowing Expanded and Spacer widgets. Perfect for responsive layouts that need to fill available space."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
repository: https://github.com/rahul-gola/flutter_expandable_column
version: 0.0.2

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
